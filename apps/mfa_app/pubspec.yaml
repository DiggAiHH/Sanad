name: mfa_app
description: Sanad MFA App - Ticket issuance with QR/NFC for reception staff
publish_to: none
version: 1.0.0

environment:
  sdk: ">=3.2.0 <4.0.0"
  flutter: ">=3.16.0"

dependencies:
  flutter:
    sdk: flutter
  sanad_core:
    path: ../../packages/core
  sanad_ui:
    path: ../../packages/ui
  flutter_riverpod: ^2.5.1
  go_router: ^13.0.0
  mobile_scanner: ^4.0.1
  nfc_manager: ^3.3.0
  qr_flutter: ^4.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1

flutter:
  uses-material-design: true
