name: admin_app
description: Sanad Admin Dashboard - Total admin control (God Mode)
publish_to: none
version: 1.0.0

environment:
  sdk: ">=3.2.0 <4.0.0"
  flutter: ">=3.16.0"

dependencies:
  flutter:
    sdk: flutter
  sanad_core:
    path: ../../packages/core
  sanad_ui:
    path: ../../packages/ui
  flutter_riverpod: ^2.5.1
  go_router: ^13.0.0
  fl_chart: ^0.66.0
  data_table_2: ^2.5.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1

flutter:
  uses-material-design: true
